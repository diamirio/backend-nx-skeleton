import { BaseCommand } from '@cenk1cenk2/boilerplate-oclif'

export class DockerCommand extends BaseCommand {
  static description = 'Create docker-compose configuration from boilerplates or NX directly.'
}
