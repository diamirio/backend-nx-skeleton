import { BaseCommand } from '@cenk1cenk2/boilerplate-oclif'

export class GitCommand extends BaseCommand {
  static description = 'Create a new preconfigured NX Workspace or refresh the configuration.'
}
