services:
  mongo:
    image: mongo:latest
    command: --directoryperdb
    {% filter indent(4, true) %}
      {% include '../common/env.yml.j2' %}
    {% endfilter %}

    ports:
      - 27017:27017
    volumes:
      - ./mongo-initialize.sh:/docker-entrypoint-initdb.d/mongo-initialize-multiple.sh:ro
