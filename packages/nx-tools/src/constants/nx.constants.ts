/**
 * Some NX constants.
 * They seem to change every minor patch XD
 */
export enum NxConstants {
  TS_CONFIG_PATH = 'tsconfig.base.json'
}

/**
 * Nx application types
 */
export enum NxProjectTypes {
  APP = 'application',
  LIB = 'library'
}
