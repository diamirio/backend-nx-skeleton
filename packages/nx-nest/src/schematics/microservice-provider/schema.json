{
  "$schema": "http://json-schema.org/schema",
  "$id": "@webundsoehne-private/nx-nest/microservice-provider",
  "title": "@webundsoehne-private/nx-nest/microservice-provider",
  "description": "Generate a common library for nestjs microservice-provider.",
  "type": "object",
  "properties": {
    "linter": {
      "description": "The tool to use for running lint checks.",
      "type": "string",
      "enum": ["eslint"],
      "default": "eslint"
    },

    "skipFormat": {
      "description": "Skip formatting after the initiation.",
      "type": "boolean",
      "aliases": ["skip-format"]
    },

    "silent": {
      "description": "Run in silent mode.",
      "type": "boolean"
    },

    "force": {
      "description": "Do not prompt for overwrites.",
      "type": "boolean"
    },

    "verbose": {
      "description": "Set this to debug mode.",
      "type": "boolean"
    }
  },
  "required": []
}
