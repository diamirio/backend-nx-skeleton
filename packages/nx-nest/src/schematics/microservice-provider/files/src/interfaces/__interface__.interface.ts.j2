{# Since this is a multiple jinja template this uses only the 'microservice' part of the variables. #}
import { MicroserviceProviderBaseMessage } from '../microservice-provider.interface'
import { {{ names.pattern }} } from '../patterns'

/**
 * Message patterns for "{{ name }}" with pattern "{{ names.pattern }}"
 */
export declare class {{ names.interface }} implements MicroserviceProviderBaseMessage<typeof {{ names.pattern }}> {
  [{{ names.pattern }}.{{ casing.default }}]: {
    response?: any | never
    request?: any | never
  }
}
