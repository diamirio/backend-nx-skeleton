{# Since this is a multiple jinja template this uses only the 'microservice' part of the variables. #}
import { {{ names.pattern }} } from '../patterns'
import { MicroserviceProviderBaseMessage, BaseMessageIndexes } from '@webundsoehne/nestjs-util-microservices'

/**
 * Request-response patterns for all message patterns of "{{ name }}" with pattern "{{ names.pattern }}"
 */
export declare class {{ names.interface }} extends BaseMessageIndexes implements MicroserviceProviderBaseMessage<{{ names.pattern }}> {
  [{{ names.pattern }}.{{ names.default }}]: (request: any) => any
}
