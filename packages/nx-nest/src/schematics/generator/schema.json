{
  "$schema": "http://json-schema.org/schema",
  "$id": "nx-nest:generate",
  "title": "Add a single file to existing nx-nest schematic.",
  "examples": [],
  "type": "object",
  "properties": {
    "directory": {
      "description": "Directory where the component is getting generated.",
      "type": "string",
      "$default": {
        "$source": "argv",
        "index": 2
      }
    },
    "force": {
      "description": "Do not prompt for overwrites.",
      "type": "boolean"
    }
  },
  "required": []
}
