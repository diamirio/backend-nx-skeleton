import { Column, Entity } from 'typeorm'

import { BaseEntity } from './util'

@Entity('{{ casing.pascal }}Entity')
export class {{ casing.pascal }}Entity extends BaseEntity<{{ casing.pascal }}Entity> {
  @Column('varchar', { length: 120 })
  dummy: string
}
