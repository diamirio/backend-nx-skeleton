{
  "$schema": "http://json-schema.org/schema",
  "$id": "nx-nest:component",
  "title": "Add a component to existing nx-nest schematic.",
  "examples": [],
  "type": "object",
  "properties": {
    "force": {
      "description": "Do not prompt for overwrites.",
      "type": "boolean"
    },

    "verbose": {
      "description": "Set this to debug mode.",
      "type": "boolean"
    }
  },
  "required": []
}
