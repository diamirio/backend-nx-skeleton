import { Injectable, Logger } from '@nestjs/common'

import { {{ casing.pascal }}Input } from './{{ name }}.input'
import { {{ casing.pascal }}Output } from './{{ name }}.output'

@Injectable()
export class {{ casing.pascal }}Service {
  private readonly logger: Logger = new Logger(this.constructor.name)

  constructor () {}

  async default (args: {{ casing.pascal }}Input): Promise<{{ casing.pascal }}Output> {
    return { response: `Hello ${args?.name}.` }
  }
}
