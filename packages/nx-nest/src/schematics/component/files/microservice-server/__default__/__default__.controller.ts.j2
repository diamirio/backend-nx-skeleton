import { Controller } from '@nestjs/common'
import { MessagePattern } from '@nestjs/microservices'

import { {{ casing.inject.microservices.interface }}, {{ casing.inject.microservices.pattern }}  } from '@{{ packageScope }}/{{ constants.MICROSERVICE_PROVIDER_PACKAGE }}'
import { DefaultMicroservice } from './{{ name }}.service'

@Controller()
export class {{ casing.pascal }}Microcontroller {
  constructor (private readonly {{ casing.camel }}Microservice: {{ casing.pascal }}Microservice) {}

  @MessagePattern({{ casing.inject.microservices.pattern }}.{{ casing.inject.microservices.default }})
  public default (
    options: {{ casing.inject.microservices.interface }}[{{ casing.inject.microservices.pattern }}.{{ casing.inject.microservices.default }}]['request']
  ): Promise<{{ casing.inject.microservices.interface }}[{{ casing.inject.microservices.pattern }}.{{ casing.inject.microservices.default }}]['response']> {
    return this.{{ casing.camel }}Microservice.default(options)
  }
}
