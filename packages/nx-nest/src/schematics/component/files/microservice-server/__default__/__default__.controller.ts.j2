import { Controller } from '@nestjs/common'
import { MessagePattern } from '@nestjs/microservices'
import { App1Message, App1MessagePattern, MessageQueueMap } from '@{{ packageScope }}/{{ constants.MICROSERVICE_PROVIDER_PACKAGE }}'
import { DefaultMicroservice } from './default.service'

@Controller()
export class DefaultMicrocontroller {
  constructor (private defaultMicroService: DefaultMicroservice) {}

  {# fix this later #}
  @MessagePattern(App1MessagePattern.APP1_DEFAULT)
  public default (options: any): Promise<any> {
    return this.defaultMicroService.default(options)
  }
}
