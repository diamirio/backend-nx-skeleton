{% if database is defined and database != enum.database.NONE %}
{% if enum.extensions.EXTERNAL_BACKEND_DATABASE in extensions and enum.dbAdapters.TYPEORM == dbAdapters %}
database:
  directory:
    entity: ./libs/{{ constants.BACKEND_DATABASE_PACKAGE }}/src/entity-typeorm/**/*.entity{.ts,.js}
    migration: migrations
    seed: seeds
{% endif %}
{% endif %}
