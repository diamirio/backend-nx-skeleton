import { MaintenanceModule } from '@diamir/nestjs-maintenance'
import { Module } from '@nestjs/common'

import { MigrationTask } from './migration.task'

@Module({
  imports: [
    MaintenanceModule.forRoot({
      application: 'Migration',
      lockfilePath: ConfigService.get('misc.lockfile')
    })
  ],
  providers: [MigrationTask],
  exports: [MigrationTask]
})
export class MigrationTaskModule {}
