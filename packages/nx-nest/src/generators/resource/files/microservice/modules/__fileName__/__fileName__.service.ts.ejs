import { Injectable, Logger } from '@nestjs/common'

@Injectable()
export class <%- resourceNames.className -%>Microservice {
  private readonly logger: Logger = new Logger(this.constructor.name)

  constructor() {}

  async default (options: any): Promise<any> {
    return 'Hello world.'
  }
}
