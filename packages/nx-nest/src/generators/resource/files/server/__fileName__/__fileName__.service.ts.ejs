import { Injectable, Logger } from '@nestjs/common'

@Injectable()
export class <%- resourceNames.className -%>Service {
  private readonly logger: Logger = new Logger(this.constructor.name)

  constructor () { /**/ }

  default (): string {
    return 'Hello <%- resourceNames.className -%>!'
  }
}
