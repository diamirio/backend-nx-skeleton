module.exports = {
  testMatch: ['<rootDir>/src/**/?(*.)+(spec|test).[jt]s'],
  transform: {
    '^.+\\.(ts|js)$': ['ts-jest', {
      tsconfig: '<rootDir>/tsconfig.json',
      diagnostics: {
        // ignore dynamic-module warning
        ignoreCodes: [151002]
      }
    }]
  }
}
