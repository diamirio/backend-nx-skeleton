<%- applicationMetadata[0].applicationImports %>

function bootstrap (): Promise<void> {
  // eslint-disable-next-line no-console
  console.log(
  // eslint-disable-next-line max-len
  `++++ ${process.env.npm_package_name} v${process.env.npm_package_version} starting on nodejs ${process.version}, environment is "${process.env.NODE_ENV}" ++++`
  )

  <%- applicationMetadata[0].applicationBootstrap %>
}

void bootstrap()
