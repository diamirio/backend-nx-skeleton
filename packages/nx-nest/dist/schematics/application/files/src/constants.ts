export enum service {
  server = 'server',
  task = 'bgtask',
  command = 'cli'
}

export enum logType {
  error = 'error',
  log = 'info',
  warn = 'warn',
  debug = 'debug',
  verbose = 'verbose'
}
