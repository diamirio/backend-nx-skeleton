{
  "name": "@webundsoehne/nestjs-util",
  "version": "1.1.1",
  "description": "NestJS skeleton util library",
  "author": "Backend <backend@webundsoehne.com>",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "prebuild": "rimraf tsconfig*.tsbuildinfo && rimraf dist/",
    "build": "tsc -p tsconfig.build.json",
    "postbuild": "tscpaths -p tsconfig.paths.json -s ./dist -o ./dist",
    "dev:build": "yarn prebuild && tsc -p tsconfig.json && yarn postbuild",
    "dev:start": "yarn dev:build && tsc-watch --noClear --onSuccess \"yarn postbuild\"",
    "lint": "prettier --loglevel warn --write src/ && eslint --ext .ts,.js,.tsx,.jsx --fix src/",
    "lint:check": "eslint --ext .ts,.js,.tsx,.jsx src/",
    "lint:staged": "lint-staged",
    "docs:toc": "markdown-toc README.md --bullets='-' -i",
    "docs:jsdoc": "typedoc src/index.ts src/microservices.ts src/graphql.ts --readme none"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true"
    }
  },
  "lint-staged": {
    "./src/*.{ts,js,tsx,jsx}": [
      "prettier --loglevel warn --write",
      "eslint --fix"
    ],
    "./src/*.{json,md}": [
      "prettier --loglevel warn --write"
    ]
  },
  "keywords": [
    "nestjs",
    "util"
  ],
  "dependencies": {
    "config": "^3.3.6",
    "fs-extra": "^10.0.0",
    "moment": "^2.29.1",
    "reflect-metadata": "^0.1.13",
    "winston": "^3.3.3"
  },
  "peerDependencies": {
    "@nestjs/common": ">= 8 < 9",
    "@nestjs/core": ">= 8 < 9",
    "@nestjs/graphql": ">= 8 < 9",
    "@nestjs/microservices": ">= 8 < 9",
    "@nestjs/swagger": ">= 5 < 6",
    "class-transformer": ">= 0.4.0",
    "class-validator": ">= 0.13.0",
    "fastify": ">= 3",
    "fastify-swagger": ">= 4.0.0 < 5",
    "graphql": ">= 15 < 16",
    "nest-schedule": ">= 0.6.0"
  },
  "devDependencies": {
    "@nestjs/common": "^8.0.2",
    "@nestjs/core": "^8.0.2",
    "@nestjs/graphql": "^9.0.4",
    "@nestjs/microservices": "^8.0.2",
    "@nestjs/swagger": "^5.0.3",
    "@nestjs/testing": "^8.0.2",
    "@types/config": "^0.0.39",
    "@types/jest": "^27.0.1",
    "@types/node": "^16.3.2",
    "@types/winston": "^2.4.4",
    "amqp-connection-manager": "^3.2.2",
    "amqplib": "^0.8.0",
    "class-transformer": "^0.4.0",
    "class-validator": "^0.13.1",
    "concurrently": "^6.2.0",
    "fastify": "^3.19.1",
    "fastify-swagger": "^4.8.3",
    "graphql": "^15.5.0",
    "nest-schedule": "^0.6.4",
    "prettier": "^2.3.0",
    "swagger-ui-express": "^4.1.5",
    "wait-on": "^6.0.0"
  },
  "engines": {
    "node": ">=10"
  }
}
