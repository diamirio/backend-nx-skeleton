{
  "$schema": "http://json-schema.org/schema",
  "id": "nx-builders:init",
  "title": "Call initiate component for the builders.",
  "examples": [],
  "type": "object",
  "properties": {
    "items": {
      "description": "The name of the application.",
      "alias": "i",
      "items": {
        "manyOf": [{
          "type": "string",
          "enum": ["ts-node-dev", "tsc"]
        }]
      }
    }
  },
  "required": []
}